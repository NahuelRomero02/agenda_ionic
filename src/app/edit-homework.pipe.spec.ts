import { EditHomeworkPipe } from './edit-homework.pipe';

describe('EditHomeworkPipe', () => {
  it('create an instance', () => {
    const pipe = new EditHomeworkPipe();
    expect(pipe).toBeTruthy();
  });
});
