import { EditHomworkPipe } from './edit-homwork.pipe';

describe('EditHomworkPipe', () => {
  it('create an instance', () => {
    const pipe = new EditHomworkPipe();
    expect(pipe).toBeTruthy();
  });
});
